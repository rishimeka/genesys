name: ci
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -e ".[dev]"
      - run: python -m pytest tests/ -v --ignore=tests/test_benchmark.py --cov=src/genesys_memory --cov-report=term-missing --cov-report=xml
      - run: python -m ruff check src/
      - run: python -m mypy src/genesys_memory --ignore-missing-imports
